import React from "react";
import ReactDOM from "react-dom/client";
import SoftcarePOS from "./SoftcarePOS";

ReactDOM.createRoot(document.getElementById("app")).render(
  <React.StrictMode>
    <SoftcarePOS />
  </React.StrictMode>
);